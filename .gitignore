# Build
build/
bin/
out/
!gradle/wrapper/gradle-wrapper.jar
.gradle/

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# Secretos — NUNCA versionar credenciales
.env
*.tfvars
!*.tfvars.example

# Terraform
# El lock file SÍ se versiona: es lo que garantiza que `terraform init`
# resuelva las mismas versiones de proveedor en cualquier máquina.
infra/.terraform/
infra/*.tfstate
infra/*.tfstate.*
infra/tfplan

# SO
.DS_Store
